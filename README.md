---
title: Financial LLM Copilot
emoji: "\U0001F4CA"
colorFrom: blue
colorTo: green
sdk: docker
pinned: false
license: openrail
app_port: 7860
---

# Financial LLM Copilot

AI-powered macroeconomic analysis covering **102 countries** across **14 sub-regions**
with **12 indicators**.

## Features

- Chat with a QLoRA fine-tuned Mistral-7B model for financial analysis
- Regional economic analysis with interactive charts
- Global country rankings by any indicator
- Side-by-side country comparisons
- Data triangulated from FRED, World Bank, and OECD

## Tech Stack

- **Frontend**: Streamlit
- **LLM**: QLoRA fine-tuned Mistral-7B-Instruct-v0.3 (LoRA `r=16`, `alpha=32`, all 7
  attention/MLP projections, 41.9M trainable params = 0.58% of base), merged and
  served as a Q4_K_M GGUF
- **Inference**: llama-cpp-python
- **Data Sources**: FRED, World Bank, OECD APIs
- **Visualization**: Plotly

## Performance

Measured on the production config (CPU-only, `n_ctx=1024`, 2 threads, Q4_K_M),
2026-08-28:

| | |
| --- | --- |
| Cold model load | ~12 s, once per container start |
| Time to first token | 5-24 s, scaling with prompt length |
| Decode | ~4.1-4.5 tokens/s |
| End-to-end answer | 35.5 s (one country) to 84.0 s (ten-country ranking) |

Numbers come from a real run on a 12th-gen mobile CPU capped to 2 threads;
the Space's 2 shared vCPUs are likely slower. Full method, per-query breakdown
and caveats: [docs/PROFILING.md](docs/PROFILING.md).

## Docs

- [docs/FINE_TUNING.md](docs/FINE_TUNING.md) — fine-tune provenance, the verified
  hyperparameters, and what is *not* recoverable
- [docs/PROFILING.md](docs/PROFILING.md) — measured end-to-end latency from a real run

Re-run the latency capture, or the regression tests:

```bash
python scripts/profile_inference.py
python test_ranking_and_context.py
```

The training corpus is synthetic (generated by `data_pipeline/`, seed 42). Real
figures come from the live API layer at inference time, not from the weights —
see [docs/FINE_TUNING.md](docs/FINE_TUNING.md) §3.
